{
  "name": "opencode-mem",
  "version": "0.1.0",
  "type": "module",
  "description": "Persistent memory plugin for OpenCode with AI compression and cross-session context",
  "author": "Ported from claude-mem by Alex Newman (@thedotmack)",
  "license": "AGPL-3.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/thedotmack/claude-mem"
  },
  "keywords": [
    "opencode",
    "ai",
    "memory",
    "context",
    "mcp",
    "persistent"
  ],
  "main": "dist/plugin.js",
  "types": "dist/plugin.d.ts",
  "bin": {
    "opencode-mem-mcp": "dist/mcp/server.js"
  },
  "files": [
    "dist/",
    "src/ui/",
    "README.md",
    "LICENSE",
    "ATTRIBUTION.md",
    ".opencode/"
  ],
  "scripts": {
    "build": "bun build src/plugin.ts --outdir dist --target=bun && bun build src/mcp/server.ts --outdir dist/mcp --target=bun && cp -r src/ui dist/",
    "test": "bun test",
    "prepublishOnly": "bun test && bun run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.26.0",
    "@opencode-ai/plugin": "^1.1.53"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "bun-types": "^1.2.0"
  },
  "peerDependencies": {
    "@opencode-ai/plugin": ">=1.0.0"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
